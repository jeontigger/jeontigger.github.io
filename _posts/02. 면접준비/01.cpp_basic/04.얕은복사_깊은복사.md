---
title: 
date: 2026-01-07 22:17:43 +09:00
categories: [면접준비, 01.cpp_basic]
published: false
tags:
    [면접준비]
---

# 🧠 C++ 얕은 복사 vs 깊은 복사 면접 질문 리스트

## ✅ 기본 개념

1. 얕은 복사(Shallow Copy)와 깊은 복사(Deep Copy)의 차이는 무엇인가요?

    - 얕은복사는 그냥 값을 그대로 복사하는 것입니다. 깊은 복사는 포인터가 가리키고 있는 지점의 힙할당 된 영역만큼 별도로 할당하며 새로 할당된 영역에 값 복사를 진행합니다.

2. 얕은 복사는 객체의 어떤 부분을 복사하나요?

    - 얕은복사는 변수가 담고있는 값을 그대로 복사합니다. 비트를 그대로 복사하기 때문에 비트복사라고도 합니다.

3. 깊은 복사는 객체의 어떤 부분을 복사하나요?

    - 객체가 갖고있는 힙 영역의 공간까지 복사합니다. 새롭게 공간을 할당하고, 해당영역과 동일한 새로운 공간이 생기도록 복사합니다.

4. 얕은 복사와 깊은 복사는 메모리 관점에서 어떻게 다른가요?

    - 얕은 복사는 새로운 공간이 필요하지 않지만, 깊은 복사는 새로운 메모리 영역이 할당됩니다.

## ✅ 문제 상황 이해

1. 얕은 복사에서 어떤 문제가 발생할 수 있나요?

    - 포인터를 얕은 복사할 때 의도하지 않은 동작이 발생할 수 있습니다. 포인터를 얕은 복사 하게 된다면 포인터에 담겨있는 즉, 가리키고 있는 변수의 주소가 복사되게 됩니다. 따라서 두 포인터 모두 같은 주소의 변수를 가리키게 됩니다. 한 포인터가 메모리를 수정, 삭제 하게 된다면 다른 포인터에서도 영향을 받는 의도하지 않은 동작이 발생할 수 있습니다.

2. 이중 해제(double free)는 왜 발생하나요?

    - 얕은 복사 또는, safe_delete를 하지 않았을 때 발생합니다. 해제하려는 자원을 가리키는 포인터가 자원이 해제됐는지 알지 못한채로 다시 한 번 해제를 시도할 때 발생합니다.

3. 댕글링 포인터(dangling pointer)는 어떤 경우에 발생하나요?

    - 댕글링 포인터도 마찬가지인 이유로 발생하게 됩니다. 해제된 포인터가 아직 유효한 포인터로 생각하고 접근하게 될 때 발생합니다.

4. 얕은 복사가 항상 잘못된 선택인가요?

    - 의도적으로 힙 영역에 있는 메모리를 동시에 가리키는 얕은복사가 필요할 때에도 있습니다. 예를들면 shared_ptr의 카운트를 관리하는 제어블록과 자원도 별도로 생성되어서는 안되기 때문에 의도적으로 얕은복사가 발생합니다.

## ✅ C++ 언어 관점

1. C++에서 기본 복사 생성자는 얕은 복사인가요?

    - 컴파일러가 생성하는 기본 복사 생성자는 얕은 복사를 진행합니다.

2. 포인터 멤버를 가진 클래스에서 기본 복사 생성자를 사용하면 어떤 문제가 생기나요?

    - 얕은 복사가 발생하여 메모리 관리 문제가 발생할 수 있습니다.

3. 깊은 복사를 구현하려면 어떤 함수들을 정의해야 하나요?

    - 복사생성자, 대입연산자, 소멸자를 정의해야 합니다.

4. 복사 생성자와 복사 대입 연산자의 역할 차이는 무엇인가요?

    - 복사 생성자는 새롭게 할당되는 객체에 사용되는 역할이고, 복사 대입 연산자는 이미 생성된 객체를 재사용하기 위해 사용되는 역할입니다. 메모리 할당이 발생하고, 메모리 할당이 발생하지 않는다는 특징이 있습니다.

## ✅ Rule of 3 / 5 / 0 연계

1. Rule of 3이란 무엇인가요?

    - 복사 생성자, 소멸자, 대입 연산자 중 하나라도 재정의가 필요하다면 세 가지 함수 모두 재정의를 해야한다는 법칙입니다.

2. Rule of 3은 얕은 복사/깊은 복사와 어떤 관련이 있나요?

    - 얕은복사로는 의도하지 않은 동작이 발생하여 깊은 복사가 필요할 때 복사 생성자나 대입 연산자를 사용하게 됩니다. 깊은 복사가 필요한 것은 힙 할당 영역에 대한 핸들링이 존재하기 때문에 소멸자에서 해제 요청이 반드시 필요합니다. 따라서 깊은 복사가 필요하다면 Rule of 3가 필요합니다.

3. Rule of 5가 등장한 이유는 무엇인가요?

    - 이동에 대한 재정의가 필요할 때 복사 생성자, 이동 생성자, 대입 연산자, 이동 연산자, 소멸자를 모두 재정의 해야한다는 법칙입니다. 이동에 대한 개념이 등장하면서 등장했습니다.

4. Rule of 0은 어떤 상황에서 권장되나요?

    RAII가 등장하면서 모든 책임을 관리 객체에 맡기는 것입니다.

## ✅ STL / 스마트 포인터

1. `std::vector`의 복사는 얕은 복사인가요, 깊은 복사인가요?

    - 깊은복사가 진행됩니다. 메모리가 별도로 할당됩니다.

2. `std::string`은 내부적으로 얕은 복사를 하나요?

    - string은 vector<char>라고 생각할 수 있습니다. vector는 내부적으로 깊은 복사를 진행합니다.

3. `std::unique_ptr`는 복사 시 어떻게 동작하나요?

    - unique_ptr은 복사가 delete되어있습니다. movem로 이동만 허용됩니다.

4. `std::shared_ptr`는 얕은 복사인가요, 깊은 복사인가요?

    - shared_ptr은 얕은 복사가 이루어져 같은 컨트롤 블록과 같은 자원을 가리키며 메모리를 아끼게됩니다.

## ✅ 실무 / 설계 판단

1. 언제 얕은 복사를 선택할 수 있나요?

    - 큰 메모리로 새로운 영역을 할당하기에 부담이 되면서, 수정이 발생하지 않는 자원인 경우에 얕은 복사로 관리할 수 있습니다.

2. 언제 깊은 복사가 반드시 필요한가요?

    - 수정이 발생하는 힙 자원의 경우에 깊은 복사가 필요합니다.

3. 성능 관점에서 얕은 복사와 깊은 복사의 차이는 무엇인가요?

    - 얕은 복사는 값 복사만 이루어지기 때문에 상당히 빠른 시간내에 이뤄집니다. 깊은 복사는 해당 포인터가 가리키는 자원의 모든 변수를 복사해야하고, 메모리 영역도 새롭게 할당해야 하므로 얕은 복사에 비하면 매우 느립니다.

4. 게임/엔진 개발에서 얕은 복사로 문제가 되는 사례를 예로 들어보세요.

    - 관리하고 있는 스프라이트가 의도하지 않은 얕은 복사된다면 다른 포인터가 해제할 때 같이 가리키고 있는 영역에서 스프라이트가 사라져 마젠타만 나온다거나 하는 문제가 발생할 수 있습니다.

## ✅ 함정 질문

1. memcpy로 객체를 복사하면 깊은 복사가 되나요?

    - memcpy는 비트 복사로 얕은 복사가 진행됩니다.

2. 얕은 복사와 참조(reference)는 같은 개념인가요?

    - 같은 개념이라고 볼 수 있습니다.

3. 복사 금지(delete copy)를 선택하는 이유는 무엇인가요?

    - unique_ptr같은 경우에 단일 소유권을 원칙으로 하는 객체이기 때문입니다.

4. 얕은 복사와 깊은 복사를 한 문장으로 요약해보세요.

    - 얕은 복사는 값을 그대로 복사하고, 깊은 복사는 메모리 영역을 새롭게 할당하여 복사합니다.

---

## 📝 GPT 피드백

### ✅ 기본 개념

**질문 1: 얕은 복사(Shallow Copy)와 깊은 복사(Deep Copy)의 차이는 무엇인가요?**
- **현재 답변**: "얕은복사는 그냥 값을 그대로 복사하는 것입니다. 깊은 복사는 포인터가 가리키고 있는 지점의 힙할당 된 영역만큼 별도로 할당하며 새로 할당된 영역에 값 복사를 진행합니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 포인터 값 복사와 실제 데이터 복사의 차이를 명확히 하면 더 좋습니다.
  - ✅ **정확한 답변**: "얕은 복사는 포인터 값만 복사하고, 깊은 복사는 실제 데이터를 새로 복사합니다."
  - 얕은 복사는 포인터 변수에 저장된 주소 값만 복사하고, 깊은 복사는 포인터가 가리키는 실제 데이터까지 새로 할당하여 복사합니다.

**질문 2: 얕은 복사는 객체의 어떤 부분을 복사하나요?**
- **현재 답변**: "얕은복사는 변수가 담고있는 값을 그대로 복사합니다. 비트를 그대로 복사하기 때문에 비트복사라고도 합니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "멤버 변수의 값(주소)"이라는 표현을 사용하면 더 명확합니다.
  - ✅ **정확한 답변**: "얕은 복사는 멤버 변수의 값(주소)을 그대로 복사합니다."
  - 포인터 멤버의 경우 포인터 변수에 저장된 주소 값만 복사됩니다.

**질문 3: 깊은 복사는 객체의 어떤 부분을 복사하나요?**
- **현재 답변**: "객체가 갖고있는 힙 영역의 공간까지 복사합니다. 새롭게 공간을 할당하고, 해당영역과 동일한 새로운 공간이 생기도록 복사합니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "포인터가 가리키는 대상까지 새로 할당해 복사"라는 표현을 사용하면 더 정확합니다.
  - ✅ **정확한 답변**: "깊은 복사는 포인터가 가리키는 대상까지 새로 할당해 복사합니다."
  - 포인터가 가리키는 힙 메모리를 새로 할당하고 그 내용을 복사합니다.

**질문 4: 얕은 복사와 깊은 복사는 메모리 관점에서 어떻게 다른가요?**
- **현재 답변**: "얕은 복사는 새로운 공간이 필요하지 않지만, 깊은 복사는 새로운 메모리 영역이 할당됩니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "메모리를 공유하고, 메모리를 분리한다"는 표현을 사용하면 더 명확합니다.
  - ✅ **정확한 답변**: "얕은 복사는 메모리를 공유하고, 깊은 복사는 메모리를 분리합니다."
  - 얕은 복사는 같은 메모리를 여러 객체가 공유하고, 깊은 복사는 각 객체가 독립적인 메모리를 가집니다.

### ✅ 문제 상황 이해

**질문 1: 얕은 복사에서 어떤 문제가 발생할 수 있나요?**
- **현재 답변**: "포인터를 얕은 복사할 때 의도하지 않은 동작이 발생할 수 있습니다. 포인터를 얕은 복사 하게 된다면 포인터에 담겨있는 즉, 가리키고 있는 변수의 주소가 복사되게 됩니다. 따라서 두 포인터 모두 같은 주소의 변수를 가리키게 됩니다. 한 포인터가 메모리를 수정, 삭제 하게 된다면 다른 포인터에서도 영향을 받는 의도하지 않은 동작이 발생할 수 있습니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "하나의 자원을 여러 객체가 공유해 이중 해제 문제가 발생할 수 있다"는 표현을 사용하면 더 포괄적입니다.
  - ✅ **정확한 답변**: "하나의 자원을 여러 객체가 공유해 이중 해제 문제가 발생할 수 있습니다."
  - 여러 객체가 같은 메모리를 공유하면 한 객체가 해제한 후 다른 객체가 다시 해제하려고 할 때 이중 해제 오류가 발생합니다.

**질문 2: 이중 해제(double free)는 왜 발생하나요?**
- **현재 답변**: "얕은 복사 또는, safe_delete를 하지 않았을 때 발생합니다. 해제하려는 자원을 가리키는 포인터가 자원이 해제됐는지 알지 못한채로 다시 한 번 해제를 시도할 때 발생합니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "같은 메모리를 두 번 해제하려 할 때 발생한다"는 표현을 사용하면 더 간결합니다.
  - ✅ **정확한 답변**: "같은 메모리를 두 번 해제하려 할 때 발생합니다."
  - 얕은 복사로 같은 메모리를 가리키는 여러 포인터가 있을 때, 각각이 해제를 시도하면 발생합니다.

**질문 3: 댕글링 포인터(dangling pointer)는 어떤 경우에 발생하나요?**
- **현재 답변**: "댕글링 포인터도 마찬가지인 이유로 발생하게 됩니다. 해제된 포인터가 아직 유효한 포인터로 생각하고 접근하게 될 때 발생합니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "이미 해제된 메모리를 참조할 때 발생한다"는 표현을 사용하면 더 명확합니다.
  - ✅ **정확한 답변**: "이미 해제된 메모리를 참조할 때 발생합니다."
  - 한 포인터가 메모리를 해제한 후, 다른 포인터가 그 메모리에 접근하려고 할 때 발생합니다.

**질문 4: 얕은 복사가 항상 잘못된 선택인가요?**
- **현재 답변**: "의도적으로 힙 영역에 있는 메모리를 동시에 가리키는 얕은복사가 필요할 때에도 있습니다. 예를들면 shared_ptr의 카운트를 관리하는 제어블록과 자원도 별도로 생성되어서는 안되기 때문에 의도적으로 얕은복사가 발생합니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "읽기 전용 공유 등 의도가 명확하다면 사용할 수 있다"는 표현을 사용하면 더 포괄적입니다.
  - ✅ **정확한 답변**: "읽기 전용 공유 등 의도가 명확하다면 사용할 수 있습니다."
  - `shared_ptr`처럼 참조 카운트로 관리하거나, 읽기 전용 데이터를 공유할 때는 의도적으로 얕은 복사를 사용할 수 있습니다.

### ✅ C++ 언어 관점

**질문 1: C++에서 기본 복사 생성자는 얕은 복사인가요?**
- **현재 답변**: "컴파일러가 생성하는 기본 복사 생성자는 얕은 복사를 진행합니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "멤버 단위 복사로 인해 얕은 복사"라는 표현을 사용하면 더 정확합니다.
  - ✅ **정확한 답변**: "기본 복사 생성자는 멤버 단위 복사로 인해 얕은 복사입니다."
  - 컴파일러가 생성하는 기본 복사 생성자는 각 멤버를 개별적으로 복사하므로, 포인터 멤버의 경우 주소 값만 복사됩니다.

**질문 2: 포인터 멤버를 가진 클래스에서 기본 복사 생성자를 사용하면 어떤 문제가 생기나요?**
- **현재 답변**: "얕은 복사가 발생하여 메모리 관리 문제가 발생할 수 있습니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "여러 객체가 동일한 동적 메모리를 소유하게 된다"는 표현을 사용하면 더 구체적입니다.
  - ✅ **정확한 답변**: "여러 객체가 동일한 동적 메모리를 소유하게 됩니다."
  - 여러 객체가 같은 힙 메모리를 가리키게 되어 이중 해제나 댕글링 포인터 문제가 발생할 수 있습니다.

**질문 3: 깊은 복사를 구현하려면 어떤 함수들을 정의해야 하나요?**
- **현재 답변**: "복사생성자, 대입연산자, 소멸자를 정의해야 합니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "복사 생성자와 복사 대입 연산자를 직접 정의해야 한다"는 표현을 사용하면 더 명확합니다.
  - ✅ **정확한 답변**: "복사 생성자와 복사 대입 연산자를 직접 정의해야 합니다."
  - 소멸자도 함께 정의해야 하며, 이는 Rule of 3에 해당합니다.

**질문 4: 복사 생성자와 복사 대입 연산자의 역할 차이는 무엇인가요?**
- **현재 답변**: "복사 생성자는 새롭게 할당되는 객체에 사용되는 역할이고, 복사 대입 연산자는 이미 생성된 객체를 재사용하기 위해 사용되는 역할입니다. 메모리 할당이 발생하고, 메모리 할당이 발생하지 않는다는 특징이 있습니다."
- **피드백**:
  - ⚠️ **부정확**: "메모리 할당이 발생하고, 메모리 할당이 발생하지 않는다"는 표현이 부정확합니다. 복사 대입 연산자도 기존 자원을 해제하고 새로 할당할 수 있습니다.
  - ✅ **정확한 답변**: "복사 생성자는 새 객체 생성 시, 대입 연산자는 기존 객체에 복사할 때 호출됩니다."
  - 복사 생성자는 새 객체를 생성할 때 호출되고, 복사 대입 연산자는 이미 생성된 객체에 값을 복사할 때 호출됩니다. 복사 대입 연산자는 기존 자원을 먼저 해제한 후 새로 할당해야 합니다.

### ✅ Rule of 3 / 5 / 0 연계

**질문 2: Rule of 3은 얕은 복사/깊은 복사와 어떤 관련이 있나요?**
- **현재 답변**: "얕은복사로는 의도하지 않은 동작이 발생하여 깊은 복사가 필요할 때 복사 생성자나 대입 연산자를 사용하게 됩니다. 깊은 복사가 필요한 것은 힙 할당 영역에 대한 핸들링이 존재하기 때문에 소멸자에서 해제 요청이 반드시 필요합니다. 따라서 깊은 복사가 필요하다면 Rule of 3가 필요합니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "깊은 복사를 안전하게 구현하기 위해 필요하다"는 표현을 사용하면 더 간결합니다.
  - ✅ **정확한 답변**: "깊은 복사를 안전하게 구현하기 위해 필요합니다."
  - 깊은 복사를 구현하면 동적 메모리를 관리해야 하므로, 복사 생성자, 복사 대입 연산자, 소멸자를 모두 정의해야 합니다.

**질문 3: Rule of 5가 등장한 이유는 무엇인가요?**
- **현재 답변**: "이동에 대한 재정의가 필요할 때 복사 생성자, 이동 생성자, 대입 연산자, 이동 연산자, 소멸자를 모두 재정의 해야한다는 법칙입니다. 이동에 대한 개념이 등장하면서 등장했습니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "이동 생성자/이동 대입으로 불필요한 깊은 복사를 줄이기 위해서"라는 표현을 사용하면 더 명확합니다.
  - ✅ **정확한 답변**: "이동 생성자/이동 대입으로 불필요한 깊은 복사를 줄이기 위해서입니다."
  - C++11에서 이동 의미론이 도입되면서, 불필요한 깊은 복사를 피하고 효율적으로 자원을 이동하기 위해 Rule of 5가 등장했습니다.

**질문 4: Rule of 0은 어떤 상황에서 권장되나요?**
- **현재 답변**: "RAII가 등장하면서 모든 책임을 관리 객체에 맡기는 것입니다."
- **피드백**:
  - 💡 **보완**: 답변이 불완전합니다. "자원 관리를 STL과 RAII에 맡길 수 있을 때 권장된다"는 표현을 사용하면 더 명확합니다.
  - ✅ **정확한 답변**: "자원 관리를 STL과 RAII에 맡길 수 있을 때 권장됩니다."
  - 스마트 포인터나 STL 컨테이너를 사용하여 자원 관리를 위임하면, 복사 생성자 등을 직접 정의할 필요가 없습니다.

### ✅ STL / 스마트 포인터

**질문 2: `std::string`은 내부적으로 얕은 복사를 하나요?**
- **현재 답변**: "string은 vector<char>라고 생각할 수 있습니다. vector는 내부적으로 깊은 복사를 진행합니다."
- **피드백**:
  - ⚠️ **부정확**: "string은 vector<char>라고 생각할 수 있다"는 표현이 부정확합니다. `std::string`은 독립적인 클래스입니다.
  - ✅ **정확한 답변**: "현대 C++에서는 일반적으로 깊은 복사 또는 COW 제거 방식입니다."
  - `std::string`은 내부적으로 버퍼를 관리하며, 복사 시 새 버퍼를 할당하여 깊은 복사를 수행합니다. 과거에는 COW(Copy-On-Write) 최적화를 사용했지만, 현대 C++에서는 제거되었습니다.

**질문 3: `std::unique_ptr`는 복사 시 어떻게 동작하나요?**
- **현재 답변**: "unique_ptr은 복사가 delete되어있습니다. movem로 이동만 허용됩니다."
- **피드백**:
  - ⚠️ **부정확**: "movem"는 "move"의 오타입니다.
  - ✅ **정확한 답변**: "복사가 금지되며 소유권은 이동으로만 전달됩니다."
  - `unique_ptr`은 단일 소유권을 보장하기 위해 복사 생성자와 복사 대입 연산자가 `delete`되어 있으며, `move`로만 소유권을 이전할 수 있습니다.

**질문 4: `std::shared_ptr`는 얕은 복사인가요, 깊은 복사인가요?**
- **현재 답변**: "shared_ptr은 얕은 복사가 이루어져 같은 컨트롤 블록과 같은 자원을 가리키며 메모리를 아끼게됩니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "포인터 값은 복사되지만 참조 카운트를 공유한다"는 표현을 사용하면 더 정확합니다.
  - ✅ **정확한 답변**: "포인터 값은 복사되지만 참조 카운트를 공유합니다."
  - `shared_ptr`은 포인터 값(주소)은 복사되지만, 같은 자원과 제어 블록을 공유하여 참조 카운트를 관리합니다.

### ✅ 실무 / 설계 판단

**질문 1: 언제 얕은 복사를 선택할 수 있나요?**
- **현재 답변**: "큰 메모리로 새로운 영역을 할당하기에 부담이 되면서, 수정이 발생하지 않는 자원인 경우에 얕은 복사로 관리할 수 있습니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "참조 의미의 공유가 의도된 경우"라는 표현을 사용하면 더 포괄적입니다.
  - ✅ **정확한 답변**: "참조 의미의 공유가 의도된 경우입니다."
  - 읽기 전용 데이터를 공유하거나, `shared_ptr`처럼 참조 카운트로 관리할 때 얕은 복사를 사용할 수 있습니다.

**질문 2: 언제 깊은 복사가 반드시 필요한가요?**
- **현재 답변**: "수정이 발생하는 힙 자원의 경우에 깊은 복사가 필요합니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "객체 간 독립적인 소유권이 필요할 때"라는 표현을 사용하면 더 명확합니다.
  - ✅ **정확한 답변**: "객체 간 독립적인 소유권이 필요할 때입니다."
  - 각 객체가 독립적으로 자원을 소유하고 수정해야 할 때 깊은 복사가 필요합니다.

**질문 3: 성능 관점에서 얕은 복사와 깊은 복사의 차이는 무엇인가요?**
- **현재 답변**: "얕은 복사는 값 복사만 이루어지기 때문에 상당히 빠른 시간내에 이뤄집니다. 깊은 복사는 해당 포인터가 가리키는 자원의 모든 변수를 복사해야하고, 메모리 영역도 새롭게 할당해야 하므로 얕은 복사에 비하면 매우 느립니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "얕은 복사는 빠르지만 안전성 문제가 있고, 깊은 복사는 비용이 크다"는 표현을 사용하면 더 간결합니다.
  - ✅ **정확한 답변**: "얕은 복사는 빠르지만 안전성 문제가 있고, 깊은 복사는 비용이 큽니다."
  - 얕은 복사는 포인터 값만 복사하므로 빠르지만, 메모리 관리 문제가 있습니다. 깊은 복사는 메모리 할당과 복사 비용이 큽니다.

**질문 4: 게임/엔진 개발에서 얕은 복사로 문제가 되는 사례를 예로 들어보세요.**
- **현재 답변**: "관리하고 있는 스프라이트가 의도하지 않은 얕은 복사된다면 다른 포인터가 해제할 때 같이 가리키고 있는 영역에서 스프라이트가 사라져 마젠타만 나온다거나 하는 문제가 발생할 수 있습니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "리소스 핸들을 얕게 복사해 수명 관리가 꼬이는 경우"라는 표현을 사용하면 더 일반적입니다.
  - ✅ **정확한 답변**: "리소스 핸들을 얕게 복사해 수명 관리가 꼬이는 경우입니다."
  - 텍스처나 스프라이트 같은 리소스 핸들을 얕게 복사하면, 한 객체가 해제한 후 다른 객체가 접근하려고 할 때 문제가 발생합니다.

### ✅ 함정 질문

**질문 1: memcpy로 객체를 복사하면 깊은 복사가 되나요?**
- **현재 답변**: "memcpy는 비트 복사로 얕은 복사가 진행됩니다."
- **피드백**:
  - ⚠️ **부정확**: 답변이 불완전합니다. `memcpy`는 단순 메모리 복사일 뿐 객체 의미의 깊은 복사가 아닙니다.
  - ✅ **정확한 답변**: "아닙니다, 단순 메모리 복사일 뿐 객체 의미의 깊은 복사가 아닙니다."
  - `memcpy`는 메모리 블록을 비트 단위로 복사하므로, 포인터 멤버가 있으면 얕은 복사가 됩니다. 또한 생성자를 호출하지 않으므로 객체 의미의 복사가 아닙니다.

**질문 2: 얕은 복사와 참조(reference)는 같은 개념인가요?**
- **현재 답변**: "같은 개념이라고 볼 수 있습니다."
- **피드백**:
  - ❌ **오개념**: 얕은 복사와 참조는 완전히 다른 개념입니다. 얕은 복사는 복사이고, 참조는 별칭(alias)입니다.
  - ✅ **정확한 답변**: "아닙니다, 얕은 복사는 복사이고 참조는 별도의 개념입니다."
  - 얕은 복사는 객체를 복사하되 포인터 값만 복사하는 것이고, 참조는 같은 객체에 대한 별칭을 만드는 것입니다. 얕은 복사된 객체는 독립적인 객체이지만, 참조는 원본과 같은 객체입니다.

**질문 3: 복사 금지(delete copy)를 선택하는 이유는 무엇인가요?**
- **현재 답변**: "unique_ptr같은 경우에 단일 소유권을 원칙으로 하는 객체이기 때문입니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "자원의 단일 소유권을 강제하기 위해서"라는 표현을 사용하면 더 일반적입니다.
  - ✅ **정확한 답변**: "자원의 단일 소유권을 강제하기 위해서입니다."
  - `unique_ptr`처럼 단일 소유권을 보장해야 하거나, 복사가 의미 없는 객체(싱글톤 등)에서 복사를 금지합니다.

**질문 4: 얕은 복사와 깊은 복사를 한 문장으로 요약해보세요.**
- **현재 답변**: "얕은 복사는 값을 그대로 복사하고, 깊은 복사는 메모리 영역을 새롭게 할당하여 복사합니다."
- **피드백**:
  - 💡 **보완**: 답변은 맞지만 "얕은 복사는 주소만 공유하고, 깊은 복사는 데이터까지 복제한다"는 표현을 사용하면 더 명확합니다.
  - ✅ **정확한 답변**: "얕은 복사는 주소만 공유하고, 깊은 복사는 데이터까지 복제합니다."
  - 포인터 멤버의 경우, 얕은 복사는 주소 값만 복사하여 같은 메모리를 공유하고, 깊은 복사는 새 메모리를 할당하여 데이터를 복제합니다.

### 📊 전체 평가

**강점:**
- ✅ 얕은 복사와 깊은 복사의 기본 개념을 잘 이해하고 있습니다
- ✅ 이중 해제와 댕글링 포인터 문제를 정확히 파악하고 있습니다
- ✅ Rule of 3의 필요성을 이해하고 있습니다
- ✅ `shared_ptr`과 `unique_ptr`의 복사 동작을 정확히 알고 있습니다
- ✅ 실무 사례를 구체적으로 제시하고 있습니다

**개선 필요:**
- ❌ **오개념**: 
  - 얕은 복사와 참조를 같은 개념으로 생각함 → 완전히 다른 개념
- ⚠️ **부정확한 부분**: 
  - `std::string`을 `vector<char>`로 설명 → 독립적인 클래스
  - "movem" 오타 (move)
  - 복사 대입 연산자에 대한 설명 부정확 (메모리 할당이 발생하지 않는다고 설명)
  - `memcpy` 설명이 불완전함 (객체 의미의 복사가 아님)
- 💡 **개선이 필요한 부분**:
  - 얕은 복사와 깊은 복사의 차이 설명에 "포인터 값 복사 vs 실제 데이터 복사" 명시
  - Rule of 0 설명 보완 필요
  - 용어 정확성 개선 (메모리 공유/분리, 참조 카운트 등)

**우선순위:**
1. **높음**: 얕은 복사와 참조의 차이 정확히 이해하기 (면접에서 자주 나오는 함정 질문)
2. **중간**: 복사 대입 연산자의 동작 방식 정확히 이해하기 (기존 자원 해제 후 새로 할당)
3. **낮음**: 용어 정확성 개선 (`std::string` 설명, 오타 수정 등)

**특별 주의사항:**
- 얕은 복사와 참조는 완전히 다른 개념입니다. 얕은 복사는 객체를 복사하되 포인터 값만 복사하는 것이고, 참조는 같은 객체에 대한 별칭입니다. 면접에서 자주 나오는 함정 질문입니다.
- 복사 대입 연산자는 기존 자원을 먼저 해제한 후 새로 할당해야 합니다. 메모리 할당이 발생하지 않는다는 설명은 부정확합니다.
- `memcpy`는 단순 메모리 복사일 뿐 객체 의미의 깊은 복사가 아니며, 생성자를 호출하지 않습니다.

<details>
<summary>GPT 1줄 답변</summary>
<div markdown="1">

## 🧠 1줄 모범 답변

### ✅ 기본 개념

1. 얕은 복사는 포인터 값만 복사하고, 깊은 복사는 실제 데이터를 새로 복사합니다.
2. 얕은 복사는 멤버 변수의 값(주소)을 그대로 복사합니다.
3. 깊은 복사는 포인터가 가리키는 대상까지 새로 할당해 복사합니다.
4. 얕은 복사는 메모리를 공유하고, 깊은 복사는 메모리를 분리합니다.

### ✅ 문제 상황 이해

1. 하나의 자원을 여러 객체가 공유해 이중 해제 문제가 발생할 수 있습니다.
2. 같은 메모리를 두 번 해제하려 할 때 발생합니다.
3. 이미 해제된 메모리를 참조할 때 발생합니다.
4. 읽기 전용 공유 등 의도가 명확하다면 사용할 수 있습니다.

### ✅ C++ 언어 관점

1. 기본 복사 생성자는 멤버 단위 복사로 인해 얕은 복사입니다.
2. 여러 객체가 동일한 동적 메모리를 소유하게 됩니다.
3. 복사 생성자와 복사 대입 연산자를 직접 정의해야 합니다.
4. 복사 생성자는 새 객체 생성 시, 대입 연산자는 기존 객체에 복사할 때 호출됩니다.

### ✅ Rule of 3 / 5 / 0 연계

1. 소멸자, 복사 생성자, 복사 대입 연산자를 함께 정의해야 한다는 규칙입니다.
2. 깊은 복사를 안전하게 구현하기 위해 필요합니다.
3. 이동 생성자/이동 대입으로 불필요한 깊은 복사를 줄이기 위해서입니다.
4. 자원 관리를 STL과 RAII에 맡길 수 있을 때 권장됩니다.

### ✅ STL / 스마트 포인터

1. 내부 버퍼를 새로 할당하므로 깊은 복사입니다.
2. 현대 C++에서는 일반적으로 깊은 복사 또는 COW 제거 방식입니다.
3. 복사가 금지되며 소유권은 이동으로만 전달됩니다.
4. 포인터 값은 복사되지만 참조 카운트를 공유합니다.

### ✅ 실무 / 설계 판단

1. 참조 의미의 공유가 의도된 경우입니다.
2. 객체 간 독립적인 소유권이 필요할 때입니다.
3. 얕은 복사는 빠르지만 안전성 문제가 있고, 깊은 복사는 비용이 큽니다.
4. 리소스 핸들을 얕게 복사해 수명 관리가 꼬이는 경우입니다.

### ✅ 함정 질문

1. 아닙니다, 단순 메모리 복사일 뿐 객체 의미의 깊은 복사가 아닙니다.
2. 아닙니다, 얕은 복사는 복사이고 참조는 별도의 개념입니다.
3. 자원의 단일 소유권을 강제하기 위해서입니다.
4. 얕은 복사는 주소만 공유하고, 깊은 복사는 데이터까지 복제합니다.

</div>
</details>