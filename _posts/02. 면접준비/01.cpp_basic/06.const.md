---
title: 
date: 2026-01-07 22:17:43 +09:00
categories: [면접준비, 01.cpp_basic]
published: false
tags:
    [면접준비]
---

# 🧠 C++ const 위치별 의미 면접 질문 리스트

## ✅ 기본 개념

1. C++에서 `const`는 무엇을 제한하나요?

    - 상수화 한다는 의미로써 변경을 제한합니다.

2. `const`의 위치가 의미에 영향을 주는 이유는 무엇인가요?

    - 포인터에 대한 const가 위치에 영향을 줍니다. int* const는 int*에 대한 상수화이고 const int *는 const int에 대한 포인터라는 의미기 때문에 위치에 따라 의미가 달라진다고 할 수 있습니다.

## ✅ 포인터와 const

1. `const int* p`의 의미를 설명해보세요.

    - const int 자료형에 대한 포인터라는 의미입니다. 포인터가 가리키고 있는 변수의 값이 불변입니다.

2. `int* const p`의 의미를 설명해보세요.

    - int형에 대한 상수형 포인터라는 의미입니다. 포인터가 상수라서 가리키는 주소값의 변경이 불가능합니다.

3. `const int* const p`의 의미를 설명해보세요.

    - const int에 대한 const 포인터라는 의미입니다. 포인터가 가리키는 주소도 변경될 수 없으며, 포인터가 가리키는 변수의 값도 변경될 수 없습니다.

## ✅ 참조와 const

1. `const int& r`의 의미는 무엇인가요?

    - 함수 파라미터로 넘기기 위해 주로 사용되는데, 복사 비용은 줄이되, 변경은 안한다는 의미를 가집니다.

2. `int& const r`는 가능한 선언인가요?

    - 불가능합니다.

## ✅ 함수 인자 / 반환값 관점

1. 반환 타입에 `const`를 붙이는 것은 어떤 의미를 가지나요?

    - 변경할 수 없는 값을 반환한다는 의미를 가집니다. 따라서 해당 함수의 리턴은 초기화에만 가능합니다.

## ✅ 읽는 법 / 실무 판단

1. API 설계에서 `const`를 적극적으로 사용하는 이유는 무엇인가요?

    - 사용자에게 함수 파라미터로 전달되어도 변경되지 않는다는 것을 전달해줍니다.

---

## 📝 GPT 피드백

### ✅ 기본 개념

**질문 1: C++에서 `const`는 무엇을 제한하나요?**
- **현재 답변**: "상수화 한다는 의미로써 변경을 제한합니다."
- **피드백**:
  - 💡 **보완**: "값의 변경을 금지해 읽기 전용을 보장합니다"가 더 정확한 표현입니다.
  - "상수화"보다는 "읽기 전용 보장"이라는 표현이 const의 본질을 더 잘 설명합니다.

**질문 2: `const`의 위치가 의미에 영향을 주는 이유는 무엇인가요?**
- **현재 답변**: "포인터에 대한 const가 위치에 영향을 줍니다. int* const는 int*에 대한 상수화이고 const int *는 const int에 대한 포인터라는 의미기 때문에 위치에 따라 의미가 달라진다고 할 수 있습니다."
- **피드백**:
  - ⚠️ **부정확**: 설명이 맞지만 핵심 규칙이 누락되었습니다.
  - ✅ **정확한 답변**: "`const`는 바로 오른쪽 대상(없으면 왼쪽)을 제한하기 때문입니다."
  - 이 규칙을 이해하면 모든 const 선언을 쉽게 해석할 수 있습니다.

### ✅ 포인터와 const

**질문 1: `const int* p`의 의미를 설명해보세요.**
- **현재 답변**: "const int 자료형에 대한 포인터라는 의미입니다. 포인터가 가리키고 있는 변수의 값이 불변입니다."
- **피드백**:
  - 💡 **보완**: 답변이 정확하지만 더 명확하게 표현할 수 있습니다.
  - ✅ **정확한 답변**: "포인터가 가리키는 값은 수정할 수 없지만 포인터는 변경 가능합니다."
  - 포인터 자체는 변경 가능하다는 점을 명시하는 것이 중요합니다.

**질문 2: `int* const p`의 의미를 설명해보세요.**
- **현재 답변**: "int형에 대한 상수형 포인터라는 의미입니다. 포인터가 상수라서 가리키는 주소값의 변경이 불가능합니다."
- **피드백**:
  - 💡 **보완**: 답변이 정확하지만 가리키는 값의 수정 가능 여부를 명시하면 더 좋습니다.
  - ✅ **정확한 답변**: "포인터 자체는 변경할 수 없지만 가리키는 값은 수정 가능합니다."

**질문 3: `const int* const p`의 의미를 설명해보세요.**
- **현재 답변**: "const int에 대한 const 포인터라는 의미입니다. 포인터가 가리키는 주소도 변경될 수 없으며, 포인터가 가리키는 변수의 값도 변경될 수 없습니다."
- **피드백**: (정확한 답변 - 피드백 없음)

### ✅ 참조와 const

**질문 1: `const int& r`의 의미는 무엇인가요?**
- **현재 답변**: "함수 파라미터로 넘기기 위해 주로 사용되는데, 복사 비용은 줄이되, 변경은 안한다는 의미를 가집니다."
- **피드백**:
  - ⚠️ **부정확**: 사용 용도에만 초점을 맞춰서 본질적인 의미가 누락되었습니다.
  - ✅ **정확한 답변**: "참조를 통해 대상 객체를 읽기 전용으로 접근한다는 의미입니다."
  - 함수 파라미터로의 사용은 응용 예시일 뿐, 본질은 "읽기 전용 접근"입니다.

**질문 2: `int& const r`는 가능한 선언인가요?**
- **현재 답변**: "불가능합니다."
- **피드백**:
  - 💡 **보완**: 답변이 맞지만 이유를 설명하면 더 좋습니다.
  - ✅ **정확한 답변**: "아니요, 참조 자체는 재지정이 불가능하므로 const를 붙일 수 없습니다."
  - 참조는 이미 상수처럼 동작하므로 추가로 const를 붙일 필요가 없고, 문법적으로도 허용되지 않습니다.

### ✅ 함수 인자 / 반환값 관점

**질문 1: 반환 타입에 `const`를 붙이는 것은 어떤 의미를 가지나요?**
- **현재 답변**: "변경할 수 없는 값을 반환한다는 의미를 가집니다. 따라서 해당 함수의 리턴은 초기화에만 가능합니다."
- **피드백**:
  - ❌ **오개념**: "초기화에만 가능하다"는 설명이 부정확합니다.
  - ✅ **정확한 답변**: "호출자가 반환값을 수정하지 못하게 하거나 의도를 명확히 합니다."
  - const 반환 타입은 반환된 임시 객체를 수정하지 못하도록 하는 것이며, 초기화에만 가능한 것은 아닙니다.
  - 예: `const int func()`의 경우, `int x = func();` 뿐만 아니라 `func().someMethod()`도 가능합니다.

### ✅ 읽는 법 / 실무 판단

**질문 1: API 설계에서 `const`를 적극적으로 사용하는 이유는 무엇인가요?**
- **현재 답변**: "사용자에게 함수 파라미터로 전달되어도 변경되지 않는다는 것을 전달해줍니다."
- **피드백**:
  - ⚠️ **부정확**: 설명이 맞지만 더 포괄적인 이유가 누락되었습니다.
  - ✅ **정확한 답변**: "인터페이스 안정성과 의도 표현을 명확히 하기 위함입니다."
  - 파라미터 변경 방지뿐만 아니라, 코드의 의도를 명확히 하고 실수를 방지하며 유지보수성을 높이는 것이 목적입니다.

### 📊 전체 평가

**강점:**
- ✅ 포인터와 const의 기본적인 의미를 이해하고 있습니다.
- ✅ `const int* const`와 같은 복합 선언을 정확히 이해하고 있습니다.
- ✅ 참조에 const를 붙일 수 없다는 것을 알고 있습니다.

**개선 필요:**
- ❌ **오개념**: const 반환 타입이 "초기화에만 가능하다"는 잘못된 이해
- ⚠️ **부정확한 부분**: 
  - `const` 위치 규칙의 핵심 원리 설명 부족
  - `const int&`의 본질적 의미보다 사용 용도에만 초점
  - API 설계에서 const 사용 이유의 포괄적 이해 부족
- 💡 **개선이 필요한 부분**:
  - 포인터와 const 설명 시 양방향(포인터 변경 가능성/값 변경 가능성) 명시
  - `int& const`가 불가능한 이유 설명 추가

**우선순위:**
1. **높음**: const 반환 타입의 오개념 수정 - 면접에서 자주 나오는 주제이며 기술적으로 중요한 개념입니다.
2. **중간**: `const` 위치 규칙의 핵심 원리 이해 - 모든 const 선언을 해석하는 기초가 됩니다.
3. **낮음**: 설명의 명확성 개선 - 답변은 맞지만 더 구체적으로 표현 가능합니다.

**특별 주의사항:**
- `const` 위치 규칙("오른쪽 대상, 없으면 왼쪽")은 면접에서 자주 나오는 질문입니다.
- const 반환 타입은 임시 객체 수정 방지가 목적이며, 초기화 제한이 아닙니다.
- 참조는 이미 재지정 불가능하므로 `int& const`는 문법적으로 불가능합니다.

<details>
<summary>GPT 1줄 답변</summary>
<div markdown="1">

## 🧠 1줄 모범 답변

1. `const`는 값의 변경을 금지해 읽기 전용을 보장합니다.
2. `const`는 바로 오른쪽 대상(없으면 왼쪽)을 제한하기 때문입니다.

3. 포인터가 가리키는 값은 수정할 수 없지만 포인터는 변경 가능합니다.
4. 포인터 자체는 변경할 수 없지만 가리키는 값은 수정 가능합니다.
5. 포인터와 포인터가 가리키는 값 모두 수정할 수 없습니다.

6. 참조를 통해 대상 객체를 읽기 전용으로 접근한다는 의미입니다.
7. 아니요, 참조 자체는 재지정이 불가능하므로 const를 붙일 수 없습니다.

8. 호출자가 반환값을 수정하지 못하게 하거나 의도를 명확히 합니다.

9. 인터페이스 안정성과 의도 표현을 명확히 하기 위함입니다.

</div>
</details>