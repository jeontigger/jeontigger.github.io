---
title: 
date: 2026-01-07 22:17:43 +09:00
categories: [면접준비, 02.cpp_advence]
published: false
tags:
    [면접준비]
---

# 🧠 C++ 복사 생성자 호출 타이밍 면접 질문 리스트

## ✅ 기본 개념

1. 복사 생성자란 무엇인가요?

    - 복사 생성자란 같은 타입의 변수를 인자로 받는 생성자입니다. 복사 생성자를 별도로 만들지 않는다면 컴파일러가 기본 복사 생성자를 생성해냅니다.

2. 복사 생성자와 복사 대입 연산자의 차이는 무엇인가요?

    - 복사 생성자는 새로 만드는 변수에 사용되는 함수이고, 복사 대입 연산자는 이미 사용중인 변수에 대입하는 연산자입니다.

## ✅ 컨테이너 / 문맥별 동작

1. STL 컨테이너에 객체를 push할 때 복사 생성자는 언제 호출되나요?

    - size가 capacity보다 작다고 가정했을 때, 가장 마지막 위치에 push하려는 객체로 복사 생성자가 호출됩니다.

2. 컨테이너 재할당(reallocation) 시 복사 생성자는 언제 호출되나요?

    - 컨테이너가 재할당되면서 이동 생성자가 없는 타입의 경우에 모든 요소에 대해 복사 생성자가 호출됩니다.

## ✅ 최적화 / 예외 상황

1. RVO(Return Value Optimization)란 무엇인가요?

    - 임시객체 최적화입니다. 임시 객체를 반환하기 위해 복사 생성자가 호출되어야 하지만, 반환하는 자리에 객체를 생성하는 것으로 복사 생성자의 호출을 줄입니다.

2. RVO가 적용되면 복사 생성자는 호출되나요?

    - 호출되지 않습니다.

## ✅ const / 참조 연계

1. const 객체를 복사할 때도 복사 생성자가 호출되나요?

    - 호출됩니다.

2. 참조로 객체를 전달하면 복사 생성자가 호출되나요?

    - 호출되지 않습니다.

## ✅ 설계 / 판단

1. 복사 생성자 호출이 잦으면 어떤 문제가 생기나요?

    - 깊은 복사가 존재한다면 메모리 할당이 자꾸 발생하는 문제가 생깁니다.

2. 복사 생성자 호출을 줄이기 위한 방법에는 무엇이 있나요?

    - 이동 생성자와 이동 대입 연산자를 선언하여 이동을 사용하는 방법이 있습니다.

## ✅ 복사 생성자와 이동 생성자 선택 기준

1. 컴파일러는 복사 생성자와 이동 생성자 중 어떤 것을 선택하나요?

    - 

2. 이동 생성자가 없으면 복사 생성자가 호출되나요?

    - 

3. 복사 생성자와 이동 생성자 중 어떤 것이 성능상 유리한가요?

    - 

## ✅ 복사 생성자와 예외 안전성

1. 복사 생성자에서 예외가 발생하면 어떤 문제가 생기나요?

    - 

2. 복사 생성자를 `noexcept`로 선언할 수 있나요?

    - 

3. 복사 생성자와 예외 안전성의 관계는 무엇인가요?

    - 

## ✅ 복사 생성자와 성능 최적화

1. 복사 생성자의 성능을 최적화하는 방법은 무엇인가요?

    - 

2. 복사 생성자 호출을 최소화하는 방법은 무엇인가요?

    - 

3. 복사 생성자와 이동 생성자를 함께 사용할 때 주의사항은 무엇인가요?

    - 

## ✅ 함정 질문

1. 복사 생성자를 한 문장으로 요약해보세요.

    - 

---

## 📝 GPT 피드백

### ✅ 기본 개념

**질문 1: 복사 생성자란 무엇인가요?**
- **현재 답변**: "복사 생성자란 같은 타입의 변수를 인자로 받는 생성자입니다. 복사 생성자를 별도로 만들지 않는다면 컴파일러가 기본 복사 생성자를 생성해냅니다."
- **피드백**:
  - ⚠️ **부정확**: 정의가 불완전합니다. "같은 타입의 변수를 인자로 받는다"는 형식적 설명일 뿐, 본질이 누락되었습니다.
  - ✅ **정확한 답변**: "기존 객체를 사용해 새 객체를 생성할 때 호출되는 생성자입니다."
  - 복사 생성자의 핵심은 "기존 객체를 이용해 새 객체를 만드는 것"입니다.

**질문 2: 복사 생성자와 복사 대입 연산자의 차이는 무엇인가요?**
- **현재 답변**: "복사 생성자는 새로 만드는 변수에 사용되는 함수이고, 복사 대입 연산자는 이미 사용중인 변수에 대입하는 연산자입니다."
- **피드백**: (정확한 답변 - 피드백 없음)

### ✅ 컨테이너 / 문맥별 동작

**질문 1: STL 컨테이너에 객체를 push할 때 복사 생성자는 언제 호출되나요?**
- **현재 답변**: "size가 capacity보다 작다고 가정했을 때, 가장 마지막 위치에 push하려는 객체로 복사 생성자가 호출됩니다."
- **피드백**: (정확한 답변 - 피드백 없음)

**질문 2: 컨테이너 재할당(reallocation) 시 복사 생성자는 언제 호출되나요?**
- **현재 답변**: "컨테이너가 재할당되면서 이동 생성자가 없는 타입의 경우에 모든 요소에 대해 복사 생성자가 호출됩니다."
- **피드백**: (정확한 답변 - 피드백 없음)

### ✅ 최적화 / 예외 상황

**질문 1: RVO(Return Value Optimization)란 무엇인가요?**
- **현재 답변**: "임시객체 최적화입니다. 임시 객체를 반환하기 위해 복사 생성자가 호출되어야 하지만, 반환하는 자리에 객체를 생성하는 것으로 복사 생성자의 호출을 줄입니다."
- **피드백**: (정확한 답변 - 피드백 없음)

**질문 2: RVO가 적용되면 복사 생성자는 호출되나요?**
- **현재 답변**: "호출되지 않습니다."
- **피드백**: (정확한 답변 - 피드백 없음)

### ✅ const / 참조 연계

**질문 1: const 객체를 복사할 때도 복사 생성자가 호출되나요?**
- **현재 답변**: "호출됩니다."
- **피드백**: (정확한 답변 - 피드백 없음)

**질문 2: 참조로 객체를 전달하면 복사 생성자가 호출되나요?**
- **현재 답변**: "호출되지 않습니다."
- **피드백**: (정확한 답변 - 피드백 없음)

### ✅ 설계 / 판단

**질문 1: 복사 생성자 호출이 잦으면 어떤 문제가 생기나요?**
- **현재 답변**: "깊은 복사가 존재한다면 메모리 할당이 자꾸 발생하는 문제가 생깁니다."
- **피드백**: (정확한 답변 - 피드백 없음)

**질문 2: 복사 생성자 호출을 줄이기 위한 방법에는 무엇이 있나요?**
- **현재 답변**: "이동 생성자와 이동 대입 연산자를 선언하여 이동을 사용하는 방법이 있습니다."
- **피드백**:
  - 💡 **보완**: 답변이 맞지만 더 포괄적인 방법을 제시할 수 있습니다.
  - ✅ **정확한 답변**: "참조 전달, 이동 생성자 지원, RVO 활용이 있습니다."
  - 이동 생성자뿐만 아니라 참조 전달과 RVO도 중요한 방법입니다.

### ✅ 복사 생성자와 이동 생성자 선택 기준

**질문 1: 컴파일러는 복사 생성자와 이동 생성자 중 어떤 것을 선택하나요?**
- **현재 답변**: 답변 누락
- **피드백**:
  - ❌ **답변 필요**: 면접에서 자주 나오는 중요한 질문입니다.
  - ✅ **정확한 답변**: "lvalue일 때는 복사 생성자를, rvalue일 때는 이동 생성자를 선택합니다."
  - 값 카테고리(value category)에 따라 선택됩니다.

**질문 2: 이동 생성자가 없으면 복사 생성자가 호출되나요?**
- **현재 답변**: 답변 누락
- **피드백**:
  - ❌ **답변 필요**: 면접에서 자주 나오는 질문입니다.
  - ✅ **정확한 답변**: "네, 이동 생성자가 없으면 복사 생성자가 호출됩니다."
  - 이동 생성자가 없으면 rvalue에서도 복사 생성자가 호출됩니다.

**질문 3: 복사 생성자와 이동 생성자 중 어떤 것이 성능상 유리한가요?**
- **현재 답변**: 답변 누락
- **피드백**:
  - ❌ **답변 필요**: 성능 최적화와 관련된 중요한 질문입니다.
  - ✅ **정확한 답변**: "이동 생성자가 포인터 복사 수준으로 빠르므로 일반적으로 이동 생성자가 유리합니다."
  - 이동은 자원 소유권만 이전하므로 복사보다 훨씬 빠릅니다.

### ✅ 복사 생성자와 예외 안전성

**질문 1: 복사 생성자에서 예외가 발생하면 어떤 문제가 생기나요?**
- **현재 답변**: 답변 누락
- **피드백**:
  - ❌ **답변 필요**: 예외 안전성은 중요한 주제입니다.
  - ✅ **정확한 답변**: "부분적으로 복사된 상태가 되어 일관성이 깨질 수 있습니다."
  - 복사 중 예외 발생 시 객체의 일관성이 깨질 수 있습니다.

**질문 2: 복사 생성자를 `noexcept`로 선언할 수 있나요?**
- **현재 답변**: 답변 누락
- **피드백**:
  - ❌ **답변 필요**: 예외 안전성과 관련된 질문입니다.
  - ✅ **정확한 답변**: "가능하지만, 깊은 복사가 필요한 경우에는 어렵습니다."
  - 얕은 복사만 하는 경우에는 가능하지만, 메모리 할당이 필요한 경우에는 어렵습니다.

**질문 3: 복사 생성자와 예외 안전성의 관계는 무엇인가요?**
- **현재 답변**: 답변 누락
- **피드백**:
  - ❌ **답변 필요**: 예외 안전성은 중요한 개념입니다.
  - ✅ **정확한 답변**: "복사 생성자는 예외 안전성을 보장하기 위해 신중하게 구현해야 합니다."
  - 복사 생성자는 strong exception guarantee를 제공해야 합니다.

### ✅ 복사 생성자와 성능 최적화

**질문 1: 복사 생성자의 성능을 최적화하는 방법은 무엇인가요?**
- **현재 답변**: 답변 누락
- **피드백**:
  - ❌ **답변 필요**: 성능 최적화는 실무에서 중요합니다.
  - ✅ **정확한 답변**: "얕은 복사 가능한 경우 얕은 복사 사용, 이동 생성자 제공, 참조 전달 활용이 있습니다."
  - 상황에 따라 다양한 최적화 방법이 있습니다.

**질문 2: 복사 생성자 호출을 최소화하는 방법은 무엇인가요?**
- **현재 답변**: 답변 누락
- **피드백**:
  - ❌ **답변 필요**: 성능 최적화와 관련된 질문입니다.
  - ✅ **정확한 답변**: "참조 전달, 이동 생성자 활용, RVO 최적화를 활용합니다."
  - 여러 방법을 조합하여 사용할 수 있습니다.

**질문 3: 복사 생성자와 이동 생성자를 함께 사용할 때 주의사항은 무엇인가요?**
- **현재 답변**: 답변 누락
- **피드백**:
  - ❌ **답변 필요**: 실무에서 중요한 주의사항입니다.
  - ✅ **정확한 답변**: "이동 생성자가 있으면 rvalue에서 이동을 선택하도록 해야 합니다."
  - 이동 생성자가 있으면 rvalue에서 자동으로 이동이 선택됩니다.

### ✅ 함정 질문

**질문 1: 복사 생성자를 한 문장으로 요약해보세요.**
- **현재 답변**: 답변 누락
- **피드백**:
  - ❌ **답변 필요**: 핵심 개념을 요약하는 능력은 중요합니다.
  - ✅ **정확한 답변**: "기존 객체를 사용하여 새 객체를 생성할 때 호출되는 생성자입니다."
  - 복사 생성자의 본질을 간결하게 표현해야 합니다.

### 📊 전체 평가

**강점:**
- ✅ 복사 생성자와 복사 대입 연산자의 차이를 정확히 이해하고 있습니다.
- ✅ RVO의 개념과 동작을 정확히 이해하고 있습니다.
- ✅ const 객체와 참조 전달 시 복사 생성자 호출 여부를 정확히 알고 있습니다.
- ✅ 컨테이너에서의 복사 생성자 호출 타이밍을 이해하고 있습니다.

**개선 필요:**
- ⚠️ **부정확한 부분**:
  - 복사 생성자의 정의가 형식적이어서 본질이 누락됨
- 💡 **개선이 필요한 부분**:
  - 복사 생성자 호출을 줄이는 방법의 포괄적 설명
- ❌ **답변 누락**: 
  - 복사 생성자와 이동 생성자 선택 기준 (3개 질문)
  - 복사 생성자와 예외 안전성 (3개 질문)
  - 복사 생성자와 성능 최적화 (3개 질문)
  - 복사 생성자 요약 (1개 질문)

**우선순위:**
1. **높음**: 복사 생성자의 본질적 정의 이해 - "기존 객체를 이용해 새 객체를 만드는 것"이라는 핵심을 명확히 해야 합니다.
2. **높음**: 복사 생성자와 이동 생성자 선택 기준 - 면접에서 자주 나오는 중요한 주제입니다.
3. **중간**: 복사 생성자와 예외 안전성 - 예외 안전성은 중요한 개념입니다.
4. **중간**: 복사 생성자와 성능 최적화 - 실무에서 중요한 주제입니다.
5. **낮음**: 복사 생성자 호출을 줄이는 방법의 포괄적 설명 - 답변은 맞지만 더 포괄적으로 설명 가능합니다.

**특별 주의사항:**
- 복사 생성자의 본질은 "기존 객체를 이용해 새 객체를 만드는 것"입니다.
- 컴파일러는 lvalue일 때 복사 생성자를, rvalue일 때 이동 생성자를 선택합니다.
- 이동 생성자가 없으면 rvalue에서도 복사 생성자가 호출됩니다.
- 복사 생성자는 예외 안전성을 보장하기 위해 신중하게 구현해야 합니다.

<details>
<summary>GPT 1줄 답변</summary>
<div markdown="1">

## 📝 모범 답변

### ✅ 기본 개념

1. 기존 객체를 사용해 새 객체를 생성할 때 호출되는 생성자입니다.
2. 복사 생성자는 새 객체 생성 시, 복사 대입 연산자는 기존 객체에 대입할 때 호출됩니다.

### ✅ 컨테이너 / 문맥별 동작

3. 컨테이너 내부에 새 요소를 복사해 저장할 때 호출됩니다.
4. 기존 요소들을 새 메모리로 옮길 때 호출됩니다.

### ✅ 최적화 / 예외 상황

5. 반환 객체 생성을 최적화해 복사를 생략하는 컴파일러 최적화입니다.
6. 아니요, RVO가 적용되면 복사 생성자는 호출되지 않습니다.

### ✅ const / 참조 연계

7. 네, const 여부와 무관하게 복사 생성자는 호출됩니다.
8. 아니요, 참조 전달은 객체를 새로 생성하지 않습니다.

### ✅ 설계 / 판단

9. 불필요한 메모리 복사로 성능 저하가 발생합니다.
10. 참조 전달, 이동 생성자 지원, RVO 활용이 있습니다.

### ✅ 복사 생성자와 이동 생성자 선택 기준

11. lvalue일 때는 복사 생성자를, rvalue일 때는 이동 생성자를 선택합니다.
12. 네, 이동 생성자가 없으면 복사 생성자가 호출됩니다.
13. 이동 생성자가 포인터 복사 수준으로 빠르므로 일반적으로 이동 생성자가 유리합니다.

### ✅ 복사 생성자와 예외 안전성

14. 부분적으로 복사된 상태가 되어 일관성이 깨질 수 있습니다.
15. 가능하지만, 깊은 복사가 필요한 경우에는 어렵습니다.
16. 복사 생성자는 예외 안전성을 보장하기 위해 신중하게 구현해야 합니다.

### ✅ 복사 생성자와 성능 최적화

17. 얕은 복사 가능한 경우 얕은 복사 사용, 이동 생성자 제공, 참조 전달 활용이 있습니다.
18. 참조 전달, 이동 생성자 활용, RVO 최적화를 활용합니다.
19. 이동 생성자가 있으면 rvalue에서 이동을 선택하도록 해야 합니다.

### ✅ 함정 질문

20. 기존 객체를 사용하여 새 객체를 생성할 때 호출되는 생성자입니다.

</div>
</details>

