---
title: 
date: 2026-01-07 22:17:43 +09:00
categories: [면접준비, 02.cpp_advence]
published: false
tags:
    [면접준비]
---

# 🧠 C++ std::move 내부 동작 이해 면접 질문 리스트

## ✅ 기본 개념

1. std::move란 무엇인가요?

    - lvalue를 rvalue로 캐스팅해줍니다.

2. std::move는 실제로 객체를 이동시키나요?

    - rvalue로 캐스팅 할 뿐, 이동시키지 않습니다. 실제로 이동시키기 위해서는 move를 통해 rvalue가 된 객체를 이동 생성자나, 이동 대입 연산자를 이용해야 합니다.

3. std::move를 호출하면 객체에는 어떤 변화가 생기나요?

    - 객체에는 변화가 없습니다. 파라미터에 따라 함수를 결정할 때 rvalue로 판정됩니다.

## ✅ 타입 / 캐스팅 관점

1. std::move의 반환 타입은 무엇인가요?

    - T&& 입니다.

2. std::move와 static_cast<T&&>의 관계는 무엇인가요?

    - 같은 동작을 합니다.

3. std::move의 실제 구현은 어떻게 되어 있나요?

    - 

4. std::move는 언제 사용해야 하나요?

    - 

## ✅ std::move와 std::forward

1. std::move와 std::forward의 차이는 무엇인가요?

    - 

2. std::forward는 언제 사용하나요?

    - 

3. std::move를 perfect forwarding에 사용할 수 있나요?

    - 

## ✅ std::move 사용 시 주의사항

1. std::move를 사용한 후 원본 객체를 사용하면 어떤 문제가 발생하나요?

    - 

2. std::move를 여러 번 호출하면 어떤 일이 발생하나요?

    - 

3. const 객체에 std::move를 사용하면 어떻게 되나요?

    - 

4. std::move를 반환값에 사용할 때 주의사항은 무엇인가요?

    - 

## ✅ std::move와 성능

1. std::move가 성능에 미치는 영향은 무엇인가요?

    - 

2. std::move를 사용하지 않으면 어떤 성능 문제가 발생하나요?

    - 

3. 모든 경우에 std::move를 사용하는 것이 좋은가요?

    - 

## ✅ std::move와 예외 안전성

1. std::move를 사용할 때 예외 안전성을 고려해야 하나요?

    - 

2. std::move 후 예외가 발생하면 어떤 문제가 생기나요?

    - 

## ✅ 실무 / 설계 판단

1. std::move를 언제 사용해야 하나요?

    - 

2. std::move를 사용하지 말아야 할 경우는 언제인가요?

    - 

3. std::move를 한 문장으로 요약해보세요.

    - 

--- 

## GPT 피드백

1. move와 static_cast<T&&>와 같은 동작을 하긴 하지만, move는 정확한 rvalue_reference를 수행하는 차이가 존재함. 엄밀히 말하면 static_cast<T&&>와는 다름

<details>
<summary>GPT 1줄 답변</summary>
<div markdown="1">

## 📝 모범 답변

### ✅ 기본 개념

1. 객체를 rvalue로 캐스팅해 이동 가능함을 표현하는 함수입니다.
2. 아니요, 실제 이동은 생성자나 대입 연산자가 수행합니다.
3. std::move 자체로는 객체 상태를 바꾸지 않습니다.

### ✅ 타입 / 캐스팅 관점

4. 전달된 객체를 rvalue reference(T&&)로 반환합니다.
5. std::move는 내부적으로 static_cast<T&&>와 유사한 동작을 하지만, rvalue reference로 정확히 변환합니다.
6. `std::remove_reference_t<T>&&`로 변환하여 rvalue reference를 반환합니다.
7. 객체를 이동할 수 있도록 rvalue로 변환해야 할 때 사용합니다.

### ✅ std::move와 std::forward

8. std::move는 항상 rvalue로 변환하고, std::forward는 조건부로 rvalue/lvalue를 유지합니다.
9. perfect forwarding이 필요한 템플릿 함수에서 사용합니다.
10. 아니요, perfect forwarding에는 std::forward를 사용해야 합니다.

### ✅ std::move 사용 시 주의사항

11. 이동된 객체는 미정의 상태이므로 사용하면 안 됩니다.
12. 여러 번 호출해도 문제없지만, 이미 이동된 객체는 의미가 없습니다.
13. const 객체는 이동할 수 없으므로 복사 생성자가 호출됩니다.
14. 지역 변수를 반환할 때는 RVO가 적용되므로 불필요할 수 있습니다.

### ✅ std::move와 성능

15. 불필요한 복사를 제거하여 성능을 향상시킬 수 있습니다.
16. 불필요한 복사가 발생하여 성능 저하가 발생합니다.
17. 아니요, 이동이 의미 있는 경우에만 사용해야 합니다.

### ✅ std::move와 예외 안전성

18. 네, 이동 후 원본 객체의 상태를 고려해야 합니다.
19. 부분적으로 이동된 상태가 되어 일관성이 깨질 수 있습니다.

### ✅ 실무 / 설계 판단

20. 객체를 이동할 수 있도록 rvalue로 변환해야 할 때 사용합니다.
21. 지역 변수 반환(RVO 적용), const 객체, 이미 이동된 객체에는 사용하지 않습니다.
22. 객체를 rvalue로 변환하여 이동 가능함을 표현하는 함수입니다.

</div>
</details>

