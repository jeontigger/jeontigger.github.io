---
title: 
date: 2026-01-07 22:17:43 +09:00
categories: [면접준비, 02.cpp_advence]
published: false
tags:
    [면접준비]
---

# 🧠 C++ 템플릿 기본 문법 면접 질문 리스트

## ✅ 기본 개념

1. C++ 템플릿이란 무엇인가요?

    - 타입에 따라 코드를 해당 타입에 맞춰 생성해주는 시스템입니다.

2. 템플릿을 사용하는 이유는 무엇인가요?

    - 타입이 다르지만 하는 동작은 동일할 때 코드 타이핑의 중복을 제거하고 유지보수성을 높입니다.

## ✅ 함수 템플릿

1. 함수 템플릿의 기본 문법은 어떻게 되나요?

    - typename 후 T같은 키워드로 변경될 타입에 대해 명명하고, 변경될 타입에 해당하는 지점에 T로 사용하면 됩니다.

2. 함수 템플릿은 언제 실제 함수로 생성되나요?

    - 함수 템플릿은 실제로 함수 사용을 한다고 판정되는 컴파일 타임에 해당 타입의 함수가 없으면 생성됩니다.

3. 템플릿 인자 타입 추론은 어떻게 이루어지나요?

    - <>안에 전달된 타입으로 추론이 이뤄집니다.

## ✅ 클래스 템플릿

1. 클래스 템플릿은 언제 인스턴스화되나요?

    - 컴파일 타임에 클래스를 사용하는 객체가 존재하는 순간 해당 클래스가 존재하지 않는다면 인스턴스화 됩니다.

## ✅ 템플릿 인자 종류

1. 템플릿 인자에는 어떤 종류가 있나요?

    - 타입과 비타입이 존재합니다.

2. 비타입(non-type) 템플릿 인자는 무엇인가요?

    - 특정한 상수나 값이 될 수 있습니다.

## ✅ 특수화 개념

1. 템플릿 특수화란 무엇인가요?

    - 특정 타입에 대해 구현부가 달라질 때 사용하게 됩니다.

## ✅ 컴파일 타임 관점

1. 템플릿이 컴파일 타임 다형성을 제공한다는 의미는 무엇인가요?

    - 타입에 따라 컴파일타임에 구현부가 달라지는 것을 의미합니다. 정적 다형성으로 분류할 수 있습니다.

## ✅ 설계 판단 / 요약

1. 템플릿 사용 시 단점은 무엇인가요?

    - 바이너리 파일이 비대해질 수 있습니다. 컴파일 에러가 사람이 알아보기 어려워집니다.

2. 템플릿 기본 개념을 한 문장으로 요약해보세요.

    - 타입에 따라 코드를 작성해주는 시스템입니다.

---

## 📝 GPT 피드백

### ✅ 기본 개념

**질문 1: C++ 템플릿이란 무엇인가요?**
- **현재 답변**: "타입에 따라 코드를 해당 타입에 맞춰 생성해주는 시스템입니다."
- **피드백**:
  - 💡 **보완**: 답변이 맞지만 더 포괄적인 정의가 가능합니다.
  - ✅ **정확한 답변**: "타입이나 값을 일반화해 코드 재사용성을 높이는 기능입니다."
  - 타입뿐만 아니라 값도 일반화할 수 있습니다.

**질문 2: 템플릿을 사용하는 이유는 무엇인가요?**
- **현재 답변**: "타입이 다르지만 하는 동작은 동일할 때 코드 타이핑의 중복을 제거하고 유지보수성을 높입니다."
- **피드백**: (정확한 답변 - 피드백 없음)

### ✅ 함수 템플릿

**질문 1: 함수 템플릿의 기본 문법은 어떻게 되나요?**
- **현재 답변**: "typename 후 T같은 키워드로 변경될 타입에 대해 명명하고, 변경될 타입에 해당하는 지점에 T로 사용하면 됩니다."
- **피드백**:
  - 💡 **보완**: 답변이 맞지만 더 정확한 문법 표현이 가능합니다.
  - ✅ **정확한 답변**: "`template<typename T>` 형태로 정의합니다."
  - `template<typename T>` 또는 `template<class T>` 형태로 정의합니다.

**질문 2: 함수 템플릿은 언제 실제 함수로 생성되나요?**
- **현재 답변**: "함수 템플릿은 실제로 함수 사용을 한다고 판정되는 컴파일 타임에 해당 타입의 함수가 없으면 생성됩니다."
- **피드백**: (정확한 답변 - 피드백 없음)

**질문 3: 템플릿 인자 타입 추론은 어떻게 이루어지나요?**
- **현재 답변**: "<>안에 전달된 타입으로 추론이 이뤄집니다."
- **피드백**:
  - ⚠️ **부정확**: 설명이 부분적으로 맞지만 더 정확한 설명이 필요합니다.
  - ✅ **정확한 답변**: "함수 호출 시 전달된 인자 타입으로 추론됩니다."
  - `<>`를 명시적으로 지정할 수도 있지만, 일반적으로 함수 호출 시 전달된 인자 타입으로 자동 추론됩니다.

### ✅ 클래스 템플릿

**질문 1: 클래스 템플릿은 언제 인스턴스화되나요?**
- **현재 답변**: "컴파일 타임에 클래스를 사용하는 객체가 존재하는 순간 해당 클래스가 존재하지 않는다면 인스턴스화 됩니다."
- **피드백**: (정확한 답변 - 피드백 없음)

### ✅ 템플릿 인자 종류

**질문 1: 템플릿 인자에는 어떤 종류가 있나요?**
- **현재 답변**: "타입과 비타입이 존재합니다."
- **피드백**: (정확한 답변 - 피드백 없음)

**질문 2: 비타입(non-type) 템플릿 인자는 무엇인가요?**
- **현재 답변**: "특정한 상수나 값이 될 수 있습니다."
- **피드백**:
  - 💡 **보완**: 답변이 맞지만 더 구체적인 설명이 가능합니다.
  - ✅ **정확한 답변**: "정수, 포인터 등 컴파일타임 상수 값을 받는 템플릿 인자입니다."
  - 정수, 포인터 등 컴파일타임 상수 값을 받을 수 있습니다.

### ✅ 특수화 개념

**질문 1: 템플릿 특수화란 무엇인가요?**
- **현재 답변**: "특정 타입에 대해 구현부가 달라질 때 사용하게 됩니다."
- **피드백**: (정확한 답변 - 피드백 없음)

### ✅ 컴파일 타임 관점

**질문 1: 템플릿이 컴파일 타임 다형성을 제공한다는 의미는 무엇인가요?**
- **현재 답변**: "타입에 따라 컴파일타임에 구현부가 달라지는 것을 의미합니다. 정적 다형성으로 분류할 수 있습니다."
- **피드백**: (정확한 답변 - 피드백 없음)

### ✅ 설계 판단 / 요약

**질문 1: 템플릿 사용 시 단점은 무엇인가요?**
- **현재 답변**: "바이너리 파일이 비대해질 수 있습니다. 컴파일 에러가 사람이 알아보기 어려워집니다."
- **피드백**:
  - 💡 **보완**: 답변이 맞지만 더 정확한 표현이 가능합니다.
  - ✅ **정확한 답변**: "컴파일 시간 증가와 에러 메시지 복잡성이 있습니다."
  - "바이너리 파일이 비대해질 수 있다"보다는 "컴파일 시간 증가"가 더 정확한 표현입니다.

**질문 2: 템플릿 기본 개념을 한 문장으로 요약해보세요.**
- **현재 답변**: "타입에 따라 코드를 작성해주는 시스템입니다."
- **피드백**:
  - 💡 **보완**: 답변이 맞지만 더 포괄적인 표현이 가능합니다.
  - ✅ **정확한 답변**: "템플릿은 타입을 일반화해 컴파일 타임에 코드를 생성하는 기능입니다."
  - "코드를 작성해주는"보다는 "코드를 생성하는"이 더 정확한 표현입니다.

### 📊 전체 평가

**강점:**
- ✅ 템플릿의 기본 개념을 정확히 이해하고 있습니다.
- ✅ 템플릿 사용 이유를 정확히 이해하고 있습니다.
- ✅ 함수 템플릿과 클래스 템플릿의 인스턴스화 시점을 정확히 이해하고 있습니다.
- ✅ 템플릿 특수화의 개념을 정확히 이해하고 있습니다.
- ✅ 컴파일 타임 다형성의 개념을 정확히 이해하고 있습니다.

**개선 필요:**
- ⚠️ **부정확한 부분**:
  - 템플릿 인자 타입 추론 설명이 부정확함 (`<>` 명시적 지정에만 초점)
- 💡 **개선이 필요한 부분**:
  - 템플릿의 정의가 타입에만 국한됨 (값도 일반화 가능)
  - 함수 템플릿 문법 표현의 정확성
  - 비타입 템플릿 인자 설명의 구체성
  - 템플릿 단점 표현의 정확성
  - 템플릿 요약 표현의 정확성

**우선순위:**
1. **중간**: 템플릿 인자 타입 추론 설명 보완 - 함수 호출 시 인자 타입으로 자동 추론된다는 점을 명확히 해야 합니다.
2. **낮음**: 템플릿 정의의 포괄성 - 타입뿐만 아니라 값도 일반화할 수 있다는 점을 언급하면 좋습니다.
3. **낮음**: 표현의 정확성 개선 - 기술적으로 더 정확한 용어 사용이 필요합니다.

**특별 주의사항:**
- 템플릿 인자 타입 추론은 함수 호출 시 전달된 인자 타입으로 자동 추론됩니다. `<>`를 명시적으로 지정할 수도 있지만 일반적으로 생략합니다.
- 템플릿은 타입뿐만 아니라 값도 일반화할 수 있습니다.
- 템플릿은 컴파일 타임에 코드를 생성하는 기능입니다.

<details>
<summary>GPT 1줄 답변</summary>
<div markdown="1">

## ?? 1줄 모범 답변

1. 타입이나 값을 일반화해 코드 재사용성을 높이는 기능입니다.
2. 타입에 독립적인 코드를 작성해 중복을 줄이기 위해서입니다.

3. `template<typename T>` 형태로 정의합니다.
4. 실제 타입이 사용되는 시점에 인스턴스화됩니다.
5. 함수 호출 시 전달된 인자 타입으로 추론됩니다.

6. 해당 타입으로 객체를 생성할 때입니다.

7. 타입 인자와 비타입 인자가 있습니다.
8. 정수, 포인터 등 컴파일타임 상수 값을 받는 템플릿 인자입니다.

9. 특정 타입에 대해 다른 구현을 제공하는 것입니다.

10. 타입에 따라 다른 코드가 컴파일 타임에 선택된다는 의미입니다.

11. 컴파일 시간 증가와 에러 메시지 복잡성이 있습니다.
12. 템플릿은 타입을 일반화해 컴파일 타임에 코드를 생성하는 기능입니다.

</div>
</details>

