# 🧠 Batching 개념 설명 가능 면접 질문 리스트

## ✅ 기본 개념

1. Batching이란 무엇인가요?

    - 배칭은 여러 매쉬를 하나의 매쉬로 합쳐 drawcall을 줄이는 최적화 기법입니다.

2. Batching은 어떤 성능 문제를 해결하기 위한 기법인가요?

    - 드로우 콜의 병목을 해결합니다.

## ✅ Draw Call과의 관계 (핵심)

1. Batching은 Draw Call 수에 어떤 영향을 주나요?

    - 드로우 콜을 줄입니다. 매쉬 종류만큼 드로우 콜이 발생하는 것을 묶음 수 만큼으로 줄일 수 있습니다.

2. Batching이 CPU 성능에 유리한 이유는 무엇인가요?

    - 드로우 콜을 줄이기 때문에 CPU의 작업이 줄어듭니다.

## ✅ 동작 방식

1. Batching은 내부적으로 무엇을 “하나로” 묶나요?

    - 매쉬를 하나로 묶습니다.

2. 어떤 조건을 만족해야 Batching이 가능한가요?

    - 

## ✅ 종류 구분

1. Static Batching이란 무엇인가요?

    - 미리 매쉬를 묶어두는 것입니다.

2. Dynamic Batching이란 무엇인가요?

    - 동적으로 매쉬를 묶는 것입니다.

3. Static과 Dynamic Batching의 핵심 차이는 무엇인가요?

## ✅ 제약 / 한계

1. 서로 다른 머티리얼을 가진 객체는 왜 Batching이 어려운가요?

    - 머터리얼을 변경하게 된다면 어차피 드로우콜이 다시 발생하기 때문입니다.

2. Batching이 항상 성능을 향상시키지 않는 이유는 무엇인가요?

    - 

## ✅ 메모리 / GPU 관점

1. Static Batching의 메모리 비용은 왜 증가하나요?

    - 각각의 매쉬와 합친 매쉬 모두가 필요하기 때문입니다.

2. Dynamic Batching이 CPU 부하를 증가시킬 수 있는 이유는 무엇인가요?

    - 묶기에 적절한 매쉬를 찾고 묶어주는데 cpu 부하가 발생합니다.

## ✅ 비교 / 대안

1. Batching과 Instancing의 가장 큰 차이는 무엇인가요?

    - 배칭은 여러 매쉬를 하나로 묶는 것이고, 인스턴싱은 하나의 매쉬를 여러번 그리는 것입니다.

2. 어떤 경우에 Batching보다 Instancing이 적합한가요?

    - 같은 매쉬를 사용하는 객체가 여러개 찍혀야할 때 사용하는 것이 좋습니다.

## ✅ 요약 질문

1. Batching 개념을 한 문장으로 요약해보세요.

<details>
<summary>GPT 1줄 답변</summary>
<div markdown="1">

## ✅ 1줄 모범 답변

1. 여러 객체를 하나의 Draw Call로 묶어 그리는 기법입니다.
2. 과도한 Draw Call로 인한 CPU 병목입니다.

3. 여러 호출을 하나로 줄입니다.
4. CPU → GPU 호출 횟수가 감소하기 때문입니다.

5. 메쉬 데이터와 렌더 상태입니다.
6. 동일한 머티리얼과 파이프라인 상태입니다.

7. 변하지 않는 객체들을 미리 하나로 합치는 방식입니다.
8. 매 프레임 런타임에 묶는 방식입니다.
9. 사전 결합 vs 런타임 결합입니다.

10. 렌더 상태가 달라지기 때문입니다.
11. 메모리 증가나 CPU 결합 비용이 생길 수 있기 때문입니다.

12. 결합된 메쉬 데이터가 복제되기 때문입니다.
13. 매 프레임 정점 재결합 비용 때문입니다.

14. Batching은 서로 다른 메쉬 결합, Instancing은 동일 메쉬 반복입니다.
15. 동일 메쉬를 대량으로 그릴 때입니다.

16. Batching은 “Draw Call을 줄이기 위한 객체 결합 기법”입니다.

</div>
</details>

---

## 📝 GPT 피드백

### ✅ 기본 개념

**질문 1: Batching이란 무엇인가요?**
- **현재 답변**: "배칭은 여러 매쉬를 하나의 매쉬로 합쳐 drawcall을 줄이는 최적화 기법입니다."
- **피드백**:
  - 💡 **보완**: "여러 객체를 하나의 Draw Call로 묶어 그리는 기법입니다"
  - 현재 답변은 맞지만, "매쉬를 하나로 합친다"는 표현보다 "여러 객체를 하나의 Draw Call로 묶는다"가 더 정확한 표현입니다. Batching은 메쉬를 물리적으로 합치는 것이 아니라 Draw Call 단위로 묶는 것입니다.

### ✅ Draw Call과의 관계 (핵심)

(정확한 답변 - 피드백 생략)

### ✅ 동작 방식

**질문 1: Batching은 내부적으로 무엇을 "하나로" 묶나요?**
- **현재 답변**: "매쉬를 하나로 묶습니다."
- **피드백**:
  - 💡 **보완**: "메쉬 데이터와 렌더 상태입니다"
  - Batching은 메쉬뿐만 아니라 동일한 렌더 상태(머티리얼, 셰이더, 파이프라인 상태 등)를 가진 객체들을 묶습니다.

**질문 2: 어떤 조건을 만족해야 Batching이 가능한가요?**
- **현재 답변**: (답변 누락)
- **피드백**:
  - ❌ **답변 필요**: Batching의 핵심 조건입니다
  - ✅ **정확한 답변**: "동일한 머티리얼과 파이프라인 상태입니다"
  - Batching을 위해서는 동일한 머티리얼(텍스처, 셰이더 등)과 파이프라인 상태(블렌딩, 컬링 등)를 가져야 합니다.

### ✅ 종류 구분

**질문 1: Static Batching이란 무엇인가요?**
- **현재 답변**: "미리 매쉬를 묶어두는 것입니다."
- **피드백**:
  - 💡 **보완**: "변하지 않는 객체들을 미리 하나로 합치는 방식입니다"
  - "미리 묶어두는 것"보다 "변하지 않는 객체들을 사전에 결합"이라는 표현이 Static Batching의 특징을 더 명확히 합니다.

**질문 2: Dynamic Batching이란 무엇인가요?**
- **현재 답변**: "동적으로 매쉬를 묶는 것입니다."
- **피드백**:
  - 💡 **보완**: "매 프레임 런타임에 묶는 방식입니다"
  - "동적으로"보다 "매 프레임 런타임에"라는 표현이 Dynamic Batching의 특징을 더 명확히 합니다.

**질문 3: Static과 Dynamic Batching의 핵심 차이는 무엇인가요?**
- **현재 답변**: (답변 누락)
- **피드백**:
  - ❌ **답변 필요**: 두 방식의 핵심 차이를 이해하는 중요한 질문입니다
  - ✅ **정확한 답변**: "사전 결합 vs 런타임 결합입니다"
  - Static Batching은 미리(빌드 타임/초기화 시) 결합하고, Dynamic Batching은 런타임(매 프레임)에 결합합니다. 이것이 핵심 차이입니다.

### ✅ 제약 / 한계

**질문 1: 서로 다른 머티리얼을 가진 객체는 왜 Batching이 어려운가요?**
- **현재 답변**: "머터리얼을 변경하게 된다면 어차피 드로우콜이 다시 발생하기 때문입니다."
- **피드백**:
  - 💡 **보완**: "렌더 상태가 달라지기 때문입니다"
  - 머티리얼 변경은 렌더 상태 변경의 한 예시입니다. Batching은 동일한 렌더 상태를 가진 객체끼리만 가능하므로, 상태가 다르면 Draw Call이 분리됩니다.

**질문 2: Batching이 항상 성능을 향상시키지 않는 이유는 무엇인가요?**
- **현재 답변**: (답변 누락)
- **피드백**:
  - ❌ **답변 필요**: Batching의 트레이드오프를 이해하는 중요한 질문입니다
  - ✅ **정확한 답변**: "메모리 증가나 CPU 결합 비용이 생길 수 있기 때문입니다"
  - Static Batching은 메모리 비용이 증가하고, Dynamic Batching은 CPU 결합 비용이 발생합니다. 이 비용이 Draw Call 감소로 얻는 이득보다 클 수 있습니다.

### ✅ 메모리 / GPU 관점

**질문 1: Static Batching의 메모리 비용은 왜 증가하나요?**
- **현재 답변**: "각각의 매쉬와 합친 매쉬 모두가 필요하기 때문입니다."
- **피드백**:
  - 💡 **보완**: "결합된 메쉬 데이터가 복제되기 때문입니다"
  - "각각의 매쉬와 합친 매쉬 모두가 필요"보다는 "결합된 메쉬 데이터가 복제"된다는 표현이 더 정확합니다. Static Batching은 각 객체의 메쉬 데이터를 복사해서 결합된 메쉬를 만들기 때문에 메모리가 증가합니다.

**질문 2: Dynamic Batching이 CPU 부하를 증가시킬 수 있는 이유는 무엇인가요?**
- **현재 답변**: "묶기에 적절한 매쉬를 찾고 묶어주는데 cpu 부하가 발생합니다."
- **피드백**:
  - 💡 **보완**: "매 프레임 정점 재결합 비용 때문입니다"
  - "적절한 매쉬를 찾고 묶어주는데"보다는 "매 프레임 정점 재결합"이라는 표현이 더 구체적입니다. Dynamic Batching은 매 프레임마다 정점 데이터를 결합하는 작업을 수행하기 때문에 CPU 부하가 발생합니다.

### ✅ 비교 / 대안

**질문 1: Batching과 Instancing의 가장 큰 차이는 무엇인가요?**
- **현재 답변**: "배칭은 여러 매쉬를 하나로 묶는 것이고, 인스턴싱은 하나의 매쉬를 여러번 그리는 것입니다."
- **피드백**:
  - 💡 **보완**: "Batching은 서로 다른 메쉬 결합, Instancing은 동일 메쉬 반복입니다"
  - 현재 답변은 맞지만, "서로 다른 메쉬 vs 동일 메쉬"라는 대비를 명확히 하면 더 좋습니다. Batching은 서로 다른 메쉬들을 결합하고, Instancing은 동일한 메쉬를 여러 번 그립니다.

**질문 2: 어떤 경우에 Batching보다 Instancing이 적합한가요?**
- **현재 답변**: "같은 매쉬를 사용하는 객체가 여러개 찍혀야할 때 사용하는 것이 좋습니다."
- **피드백**:
  - 💡 **보완**: "동일 메쉬를 대량으로 그릴 때입니다"
  - 현재 답변은 맞지만, "대량으로"라는 표현을 추가하면 Instancing의 활용 시점을 더 명확히 할 수 있습니다.

### ✅ 요약 질문

**질문 1: Batching 개념을 한 문장으로 요약해보세요.**
- **현재 답변**: (답변 누락)
- **피드백**:
  - ❌ **답변 필요**: 핵심 개념을 요약하는 중요한 질문입니다
  - ✅ **정확한 답변**: "Batching은 'Draw Call을 줄이기 위한 객체 결합 기법'입니다"
  - 또는 "여러 객체를 하나의 Draw Call로 묶어 그리는 최적화 기법"이라고 설명할 수 있습니다.

### 📊 전체 평가

**강점:**
- ✅ Batching의 기본 개념 이해
- ✅ Batching이 Draw Call 병목을 해결한다는 점 이해
- ✅ Batching이 Draw Call 수를 줄인다는 점 이해
- ✅ CPU 성능 향상 이유 이해
- ✅ Batching과 Instancing의 차이 이해

**개선 필요:**
- ❌ Batching 가능 조건 답변 누락
- ❌ Static/Dynamic Batching 차이 답변 누락
- ❌ Batching 성능 트레이드오프 답변 누락
- ❌ 요약 질문 답변 누락
- 💡 Batching 정의 보완 필요 (매쉬 합치기 → Draw Call 묶기)
- 💡 Batching이 묶는 대상 보완 필요 (매쉬 → 메쉬 데이터와 렌더 상태)
- 💡 Static/Dynamic Batching 정의 보완 필요 (더 명확한 표현)
- 💡 머티리얼 차이와 Batching 관계 보완 필요 (렌더 상태 관점)
- 💡 메모리 비용 설명 보완 필요
- 💡 Dynamic Batching CPU 부하 설명 보완 필요
- 💡 Batching과 Instancing 차이 보완 필요
- 💡 Instancing 적합성 보완 필요

**우선순위:**
1. **높음**: Batching 가능 조건 (답변 누락), Static/Dynamic Batching 차이 (답변 누락), Batching 성능 트레이드오프 (답변 누락), 요약 질문 (답변 누락)
2. **중간**: Batching 정의, Batching이 묶는 대상, 머티리얼 차이와 Batching 관계
3. **낮음**: 답변의 구체성 개선

**특별 주의사항:**
- Batching은 메쉬를 물리적으로 합치는 것이 아니라, 동일한 렌더 상태를 가진 객체들을 하나의 Draw Call로 묶는 기법입니다
- Batching을 위해서는 동일한 머티리얼과 파이프라인 상태가 필요합니다
- Static Batching과 Dynamic Batching의 핵심 차이는 사전 결합 vs 런타임 결합입니다
- Batching은 메모리 증가(Static)나 CPU 결합 비용(Dynamic)이라는 트레이드오프가 있으므로 항상 성능 향상을 보장하지 않습니다

